server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myprojects
    username: root
    password: Raju@8465
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  application:
    name: productservice

 
